# Environment and secrets: never commit real coordinates or codes
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example

# Real course data seeds (fake example seeds are committed)
seeds/*.real.*

# Dependencies and build output
node_modules/
dist/
.wrangler/
.mf/

# Uploaded imagery must never enter the repo
uploads/
*.tif
*.tiff
overlay*.jpg
overlay*.jpeg
overlay*.png

# Editor and OS noise
.DS_Store
*.log
.idea/
.vscode/
